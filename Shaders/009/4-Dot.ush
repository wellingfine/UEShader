

uv = frac(uv * 10);

// float circleRadius = 0.3; // 圆的圈数会有变化
float circleRadius = 0.1;

bool pattern = (int(floor(length(uv - 0.5) / circleRadius)) % 2) == 0;

return pattern ? float3(1, 0, 0) : float3(0, 1, 0);