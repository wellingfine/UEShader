

// 测试一下
// pos.x = pos.x + 0.2;

// 注意这里已经是 float 了，所以只剩黑白
// 输出 d 就是一个黑中心的渐变圆
float d = length(pos - uv);

// 
// 做一个明显的边缘
// step(y,x)=y>x?0:1
d = step(d, 0.5);

// 或者这样
// 表示小于0.1的就返回1，即白色，所以最终得到一个黑圆
float x = d < 0.1;

return x;